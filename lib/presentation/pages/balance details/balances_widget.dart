import 'package:flutter/material.dart';

class BalancesWidget extends StatelessWidget {
  const BalancesWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
        child: Text('Balances Widget', style: TextStyle(fontSize: 24)));
  }
}
