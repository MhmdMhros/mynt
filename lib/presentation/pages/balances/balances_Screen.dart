import 'package:flutter/material.dart';

class BalancesScreen extends StatelessWidget {
  const BalancesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
        child: Text('Balances Screen', style: TextStyle(fontSize: 24)));
  }
}
